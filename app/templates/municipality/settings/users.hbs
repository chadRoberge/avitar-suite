{{!-- User Management Settings --}}
<div class="avitar-content" style="padding: var(--spacing-6); background: var(--color-gray-50);">
  <div class="avitar-card">
    <div class="avitar-card__header">
      <h1 class="avitar-card__title">User Management</h1>
      <div class="avitar-card__subtitle">Manage user accounts, roles, and permissions</div>
    </div>
    <div class="avitar-card__body">
      <p class="avitar-text-muted">Add, edit, and manage user accounts and their access permissions.</p>
      {{!-- Placeholder content - will be implemented later --}}
      <div class="avitar-empty-state">
        <i class="fas fa-users"></i>
        <span>User management settings coming soon</span>
      </div>
    </div>
  </div>
</div>