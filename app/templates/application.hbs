{{page-title "AvitarSuite"}}

{{outlet}}

{{! Global conflict resolver for local-first data sync }}
<Shared::ConflictResolver />

{{! Sync status indicator for monitoring data sync }}
<Shared::SyncStatusIndicator />

{{! Global loading overlay }}
<Shared::LoadingOverlay @isVisible={{this.loading.isLoading}} @message={{this.loading.loadingMessage}} />
